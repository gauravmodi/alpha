<h1><%= @company.name %></h1>

<p>
	Category:
	<% @company.categories.each do |category| %>
		<%= link_to category.name, category_path(category) %>
	<% end %>
</p>
<%= link_to "<< Back to Company List", companies_path %>